# define ordem 3

typedef struct no {

	int n; //Numero de chaves que o nó possui
	int chaves[ordem - 1]; // Um vetor que pode armazenar todas as chaves 
	struct no *p[ordem]; // Numero de ponteiro que o nó possui

}node


